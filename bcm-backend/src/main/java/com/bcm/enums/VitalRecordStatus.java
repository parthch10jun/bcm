package com.bcm.enums;

/**
 * Status of a Vital Record
 */
public enum VitalRecordStatus {
    ACTIVE,      // Currently in use
    ARCHIVED,    // No longer actively used but retained
    INACTIVE     // Temporarily not in use
}

