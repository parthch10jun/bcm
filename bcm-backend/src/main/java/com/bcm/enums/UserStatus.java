package com.bcm.enums;

/**
 * User Status - Represents the employment status of a user
 */
public enum UserStatus {
    ACTIVE,      // Currently employed and active
    INACTIVE,    // Temporarily inactive (leave, sabbatical)
    TERMINATED   // Employment terminated
}

